spring:
  application:
    name: portfolio_api1
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: Ali200aa
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        logging:
          level:
            org.hibernate.SQL:
              DEBUG org.hibernate.type.descriptor.sql.BasicBinder: TRACE













